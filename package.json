{
  "name": "calico",
  "version": "1.0.0",
  "description": "Calico is a static-site generator that I made because I'm tired of using the ones already on the internet.",
  "main": "bin/index.js",
  "scripts": {
    "ts": "npx tsc bin/index.ts || exit 1",
    "global": "npm i -g",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Leon Truong",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "bin": {
    "cali": "./bin/index.js"
  }
}
